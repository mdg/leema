
use leema::inter::{Interloader};
use leema::program;
use leema::log;

use std::io::{stderr, Write};


pub fn module(prog: &mut program::Lib, inter: Interloader, modfile: &str)
{
    vout!("typecheck module({}\n", modfile);
    // let modname = module::name(module_or_file);
    // let mo
    //if prog.module
}
