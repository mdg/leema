
import /io.print
import /map


func main ->
    let m1 := {}
    let m2 := map.set(m1, "tacos", 4)
    let cont := map.has_key(m2, "tacos")
    print("map contains tacos? $cont\n")
    let len := map.len(m2)
    print("map length is $len\n")

    let v4 := map.get(m2, "tacos")
    print("map[tacos] = $v4\n")
--
